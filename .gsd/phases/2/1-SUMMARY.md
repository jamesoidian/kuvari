# Phase 2 Summary: Service Layer

## Completed Tasks
- **Create TtsService class**: Created `lib/services/tts_service.dart` with a `TtsService` class. It initializes `FlutterTts`, maps the application language codes (`fi`, `sv`, `en`) to TTS locales (`fi-FI`, `sv-SE`, `en-US`), and implements the `speak` method to trigger text-to-speech.

## Changes Made
- Created new file `lib/services/tts_service.dart`.

All tasks completed successfully.
