---
phase: 02-home-guidance-collection-queue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/l10n/intl_fi.arb
  - lib/l10n/intl_sv.arb
  - lib/l10n/intl_en.arb
  - lib/widgets/empty_queue_placeholder.dart
  - test/widgets/empty_queue_placeholder_test.dart
autonomous: true
requirements:
  - JONO-01
user_setup: []

must_haves:
  truths:
    - "D-12: Localized empty queue guidance string exists in Finnish, Swedish, and English ARB files and is generated into AppLocalizations."
    - "D-13: Localized parametric story playback label with count exists in Finnish, Swedish, and English ARB files."
    - "D-01, D-02: EmptyQueuePlaceholder widget renders the collection bookmark icon and localized guidance text in a bordered card matching carousel dimensions (~88-90px)."
    - "D-11: EmptyQueuePlaceholder is implemented as a dedicated modular widget in lib/widgets/empty_queue_placeholder.dart with isolated tests."
    - "D-14: EmptyQueuePlaceholder invokes onTap callback when tapped and provides assistive Semantics for screen readers."
  artifacts:
    - lib/widgets/empty_queue_placeholder.dart
    - test/widgets/empty_queue_placeholder_test.dart
  key_links:
    - "lib/widgets/empty_queue_placeholder.dart uses AppLocalizations.of(context)!.emptyQueueGuidance (D-12)"
    - "lib/widgets/empty_queue_placeholder.dart accepts VoidCallback? onTap and wraps container in InkWell and Semantics (D-14)"
---

<objective>
Define the localization keys (D-12, D-13) and create the modular `EmptyQueuePlaceholder` widget (D-01, D-02, D-11, D-14) with isolated widget test suite.

Purpose: Provide an instructive empty state placeholder for the collection queue (JONO-01) that matches the active carousel's dimensions to eliminate layout shifts, and prepare localized parametric copy for both the placeholder and the upcoming extended FAB.
Output: ARB entries in FI/SV/EN, `lib/widgets/empty_queue_placeholder.dart`, and `test/widgets/empty_queue_placeholder_test.dart`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-home-guidance-collection-queue/02-CONTEXT.md
@.planning/phases/02-home-guidance-collection-queue/02-RESEARCH.md
@lib/l10n/intl_fi.arb
@lib/widgets/selected_images_carousel.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add localization keys for empty queue guidance and story playback count (D-12, D-13)</name>
  <files>lib/l10n/intl_fi.arb, lib/l10n/intl_sv.arb, lib/l10n/intl_en.arb</files>
  <read_first>lib/l10n/intl_fi.arb, lib/l10n/intl_sv.arb, lib/l10n/intl_en.arb</read_first>
  <action>
Add two new keys to each ARB file according to decisions D-12 and D-13:

1. `emptyQueueGuidance` (D-12):
   - Finnish (`intl_fi.arb`): `"Valitsemasi kuvat tulevat tähän jonoon. Voit koota viestin, päiväjärjestyksen tai valintataulun."`
   - Swedish (`intl_sv.arb`): `"Dina valda bilder hamnar i denna sekvens. Du kan skapa ett meddelande, en dagsordning eller en valbricka."`
   - English (`intl_en.arb`): `"Your selected images appear in this queue. You can compose a message, daily schedule, or choice board."`
   Include metadata `@emptyQueueGuidance` with description.

2. `viewImageStoryWithCount` (D-13):
   - Finnish (`intl_fi.arb`): `"Näytä kuvajono ({count})"`
   - Swedish (`intl_sv.arb`): `"Visa bildsekvens ({count})"`
   - English (`intl_en.arb`): `"View story ({count})"`
   Include metadata `@viewImageStoryWithCount` with description and placeholder definition:
   ```json
   "@viewImageStoryWithCount": {
     "description": "Label for the extended FAB showing count of queued images",
     "placeholders": {
       "count": {
         "type": "int",
         "example": "3"
       }
     }
   }
   ```

Then run `flutter gen-l10n` to regenerate `AppLocalizations`.
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test</automated>
    <fails_when>non-zero exit code or compilation error in generated localizations</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/l10n/intl_fi.arb` contains `"emptyQueueGuidance": "Valitsemasi kuvat tulevat tähän jonoon. Voit koota viestin, päiväjärjestyksen tai valintataulun."` (D-12)
    - `lib/l10n/intl_fi.arb` contains `"viewImageStoryWithCount": "Näytä kuvajono ({count})"` (D-13)
    - `lib/l10n/intl_sv.arb` contains `"emptyQueueGuidance": "Dina valda bilder hamnar i denna sekvens. Du kan skapa ett meddelande, en dagsordning eller en valbricka."` (D-12)
    - `lib/l10n/intl_sv.arb` contains `"viewImageStoryWithCount": "Visa bildsekvens ({count})"` (D-13)
    - `lib/l10n/intl_en.arb` contains `"emptyQueueGuidance": "Your selected images appear in this queue. You can compose a message, daily schedule, or choice board."` (D-12)
    - `lib/l10n/intl_en.arb` contains `"viewImageStoryWithCount": "View story ({count})"` (D-13)
    - `flutter gen-l10n` generates `emptyQueueGuidance` and `viewImageStoryWithCount` methods on `AppLocalizations`.
  </acceptance_criteria>
  <done>Localization keys D-12 and D-13 are committed and generated code compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement EmptyQueuePlaceholder widget (D-01, D-02, D-11, D-14)</name>
  <files>lib/widgets/empty_queue_placeholder.dart</files>
  <read_first>lib/widgets/selected_images_carousel.dart, .planning/phases/02-home-guidance-collection-queue/02-RESEARCH.md</read_first>
  <action>
Create `lib/widgets/empty_queue_placeholder.dart` (D-11):
- Widget class: `EmptyQueuePlaceholder extends StatelessWidget`
- Constructor parameter: `final VoidCallback? onTap;`
- Sizing (D-01): Total height constrained to ~88-90px (e.g. `SizedBox(height: 88, child: ...)`), matching the outer footprint of `SelectedImagesCarousel`.
- Decoration (D-01):
  - Rounded border: `BorderRadius.circular(8.0)`
  - Border: `Border.all(color: Colors.teal.shade200, width: 1.5)`
  - Background: subtle teal tint: `Colors.teal.withValues(alpha: 0.05)`
  - Padding: `const EdgeInsets.symmetric(horizontal: 12.0, vertical: 8.0)`
- Child layout: `Row` containing:
  - Icon container (D-02): `Container` with subtle teal circular/rounded background, padding, and `Icon(Icons.collections_bookmark_outlined, color: Colors.teal, size: 28)`.
  - Spacing: `const SizedBox(width: 12)`
  - Text (D-12): `Expanded` with `Text(AppLocalizations.of(context)!.emptyQueueGuidance, style: TextStyle(fontSize: 13, color: Colors.teal.shade900, height: 1.3), maxLines: 3, overflow: TextOverflow.ellipsis)`
- Tap interaction & Semantics (D-14):
  - Wrap in `Semantics(button: true, label: AppLocalizations.of(context)!.emptyQueueGuidance, hint: AppLocalizations.of(context)!.searchHint, child: InkWell(onTap: onTap, borderRadius: BorderRadius.circular(8.0), child: ...))`
  </action>
  <verify>
    <automated>flutter analyze lib/widgets/empty_queue_placeholder.dart</automated>
    <fails_when>non-zero exit code or analyzer warnings</fails_when>
  </verify>
  <acceptance_criteria>
    - `lib/widgets/empty_queue_placeholder.dart` exists and exports `EmptyQueuePlaceholder` (D-11).
    - Height of widget matches carousel footprint ~88-90px (D-01).
    - Uses `Icons.collections_bookmark_outlined` in theme teal (D-02).
    - Uses `AppLocalizations.of(context)!.emptyQueueGuidance` (D-12).
    - Handles `onTap` via `InkWell`.
    - Wrapped in accessibility `Semantics` (D-14).
    - `flutter analyze` passes with zero issues.
  </acceptance_criteria>
  <done>EmptyQueuePlaceholder widget is implemented cleanly with zero analyzer warnings.</done>
</task>

<task type="auto">
  <name>Task 3: Create isolated widget tests for EmptyQueuePlaceholder (D-01, D-02, D-11, D-14)</name>
  <files>test/widgets/empty_queue_placeholder_test.dart</files>
  <read_first>test/widgets/image_grid_test.dart, lib/widgets/empty_queue_placeholder.dart</read_first>
  <action>
Create `test/widgets/empty_queue_placeholder_test.dart` (D-11):
- Test 1: Renders icon `Icons.collections_bookmark_outlined` (D-02) and localized Finnish text (D-12) when locale is `fi`.
- Test 2: Renders localized Swedish text when locale is `sv` (D-12).
- Test 3: Renders localized English text when locale is `en` (D-12).
- Test 4: Tapping the placeholder triggers the `onTap` callback (D-14).
- Test 5: Verifies Semantics node has `isButton: true` and contains the expected accessibility guidance label (D-14).
  </action>
  <verify>
    <automated>flutter test test/widgets/empty_queue_placeholder_test.dart</automated>
    <fails_when>non-zero exit code or failing tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `test/widgets/empty_queue_placeholder_test.dart` contains test suite covering all 3 locales, tap interaction, and semantics.
    - All tests pass: `flutter test test/widgets/empty_queue_placeholder_test.dart` exits with 0.
  </acceptance_criteria>
  <done>Unit/widget test suite for EmptyQueuePlaceholder passes completely.</done>
</task>

</tasks>
