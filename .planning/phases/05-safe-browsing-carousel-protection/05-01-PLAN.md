---
phase: 5
plan: 1
wave: 1
depends_on: []
files_modified:
  - lib/widgets/selected_images_carousel.dart
  - lib/pages/saved_image_stories_page.dart
  - test/widgets/selected_images_carousel_test.dart
  - test/pages/saved_image_stories_page_test.dart
autonomous: true
user_setup: []

must_haves:
  truths:
    - "SelectedImagesCarousel renders a standard horizontal ListView without drag-to-reorder gesture recognizers when isReorderable is false"
    - "SelectedImagesCarousel retains ReorderableListView by default (isReorderable: true) preserving existing HomePage reordering"
    - "SavedImageStoriesPage passes isReorderable: false and showClearButton: false to SelectedImagesCarousel"
    - "Saved stories carousels cannot be reordered by dragging, and individual symbol close buttons are absent"
  artifacts:
    - "lib/widgets/selected_images_carousel.dart"
    - "lib/pages/saved_image_stories_page.dart"
    - "test/widgets/selected_images_carousel_test.dart"
    - "test/pages/saved_image_stories_page_test.dart"
  key_links:
    - "SavedImageStoriesPage subtitle -> SelectedImagesCarousel(isReorderable: false, showClearButton: false)"
---

# Plan 05.01: Safe Browsing & Carousel Protection

<objective>
Protect the saved image stories browsing experience by making the carousel purely scrollable without drag-to-reorder (`SAFE-01`) and ensuring symbol deletion buttons remain absent (`SAFE-02`), preventing accidental reordering or deletion while viewing stories.
</objective>

<context>
Load for context:
- `.planning/phases/05-safe-browsing-carousel-protection/05-CONTEXT.md`
- `.planning/REQUIREMENTS.md`
- `lib/widgets/selected_images_carousel.dart`
- `lib/pages/saved_image_stories_page.dart`
- `test/widgets/selected_images_carousel_test.dart`
- `test/pages/saved_image_stories_page_test.dart`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isReorderable support to SelectedImagesCarousel with unit tests</name>
  <files>
    lib/widgets/selected_images_carousel.dart
    test/widgets/selected_images_carousel_test.dart
  </files>
  <action>
    1. Update `SelectedImagesCarousel` in `lib/widgets/selected_images_carousel.dart`:
       - Add `final bool isReorderable;` property with default `true` in constructor.
       - Make `onReorder` optional (`final Function(int, int)? onReorder;`).
       - In `build()`, conditionally render:
         - When `isReorderable == true`: `ReorderableListView(scrollController: _scrollController, scrollDirection: Axis.horizontal, onReorder: widget.onReorder ?? (a, b) {}, children: ...)`
         - When `isReorderable == false`: `ListView(scrollController: _scrollController, scrollDirection: Axis.horizontal, children: ...)`
       - Ensure `_scrollController`, scroll indicators, and image item rendering work identically across both modes.
       - AVOID: Modifying the `HomePage` carousel behavior or breaking existing `SelectedImagesCarousel` consumers where `isReorderable` defaults to `true`.
    2. Add unit tests in `test/widgets/selected_images_carousel_test.dart`:
       - Verify that `isReorderable: false` renders a standard `ListView` and does NOT contain `ReorderableListView`.
       - Verify that `showClearButton: false` hides all close buttons (`Icons.close`) and the clear button (`Icons.delete_sweep`).
       - Verify default behavior retains `ReorderableListView`.
  </action>
  <verify>
    flutter test test/widgets/selected_images_carousel_test.dart
  </verify>
  <done>
    SelectedImagesCarousel supports isReorderable: false rendering a standard ListView, and all carousel widget tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure SavedImageStoriesPage with protected carousel and verify integration</name>
  <files>
    lib/pages/saved_image_stories_page.dart
    test/pages/saved_image_stories_page_test.dart
  </files>
  <action>
    1. In `lib/pages/saved_image_stories_page.dart`:
       - Update the `SelectedImagesCarousel` instantiation in `ListTile.subtitle`:
         - Set `isReorderable: false`.
         - Ensure `showClearButton: false` is maintained.
       - AVOID: Touching the tag management logic, dismissible story deletion, or play story navigation.
    2. In `test/pages/saved_image_stories_page_test.dart`:
       - Add test verifying that saved story carousels on `SavedImageStoriesPage` do not contain `ReorderableListView` (`find.byType(ReorderableListView)` finds nothing).
       - Verify that no image delete buttons (`Icons.close`) are rendered in the saved stories list.
       - Verify non-regression of story name display, tag guidance, and search.
    3. Run full test suite across the app.
  </action>
  <verify>
    flutter test test/pages/saved_image_stories_page_test.dart && flutter test
  </verify>
  <done>
    Saved stories carousel is locked down to read-only scroll without reordering or deletion controls, and all project tests pass.
  </done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] `flutter test test/widgets/selected_images_carousel_test.dart` passes.
- [ ] `flutter test test/pages/saved_image_stories_page_test.dart` passes.
- [ ] `flutter test` passes all tests cleanly with zero regressions.
- [ ] `ReorderableListView` is completely absent from `SavedImageStoriesPage`.
</verification>

<success_criteria>
- [ ] `SAFE-01`: Drag reordering is disabled on `SavedImageStoriesPage` carousel via `isReorderable: false`.
- [ ] `SAFE-02`: Individual image delete buttons remain absent from `SavedImageStoriesPage` carousel.
- [ ] Default reorderable behavior on `HomePage` remains 100% intact.
- [ ] All automated tests pass cleanly.
</success_criteria>
