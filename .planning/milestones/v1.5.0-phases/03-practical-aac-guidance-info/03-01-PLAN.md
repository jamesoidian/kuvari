---
phase: 03-practical-aac-guidance-info
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/l10n/intl_fi.arb
  - lib/l10n/intl_sv.arb
  - lib/l10n/intl_en.arb
  - lib/pages/info_page.dart
  - test/pages/info_page_test.dart
autonomous: true
requirements:
  - GUIDE-01
  - GUIDE-02
user_setup: []

must_haves:
  truths:
    - "D-01, GUIDE-02: InfoPage organizes content into three distinct Material 3 Card sections (Quick Start, Practical Use Cases, About & Attributions) inside a scrollable ListView."
    - "D-02, GUIDE-01: Practical Use Cases section features concrete guidance for three core AAC scenarios: Making Choices, Daily Routines & Sequences, and Pointing & Speech Modeling."
    - "D-03: Cards use rounded corners, subtle border, teal accent icons (Icons.bolt_outlined, Icons.forum_outlined, Icons.info_outline), and clear typography."
    - "D-04: Full localization across Finnish, Swedish, and English in plain-language encouraging tone."
    - "D-05: Preserves all existing external links (Papunet, OpenSymbols, Rinnekodit, Creative Commons) opened safely via url_launcher."
    - "InfoPage supports dependency-injected urlLauncher callback for robust, isolated widget testing without platform channel errors."
  artifacts:
    - lib/l10n/intl_fi.arb
    - lib/l10n/intl_sv.arb
    - lib/l10n/intl_en.arb
    - lib/pages/info_page.dart
    - test/pages/info_page_test.dart
  key_links:
    - "lib/pages/info_page.dart uses new AppLocalizations keys for Quick Start and Practical Use Cases (D-04)"
    - "lib/pages/info_page.dart hosts ListView with 3 distinct Material 3 Cards (D-01, D-03)"
    - "lib/pages/info_page.dart preserves url_launcher calls with externalApplication mode (D-05)"
    - "test/pages/info_page_test.dart injects mock urlLauncher callback and verifies URI dispatching"
---

<objective>
Refactor `InfoPage` from an unscrollable static text screen into an accessible, structured, and practical AAC communication guide organized into three themed Material 3 Cards (Quick Start, Practical Use Cases, About & Attributions).

Purpose: Fulfill requirements GUIDE-01 and GUIDE-02 by providing actionable AAC communication guidance (choice-making, daily routines, speech modeling) alongside transparent licensing and source attributions, fully localized in Finnish, Swedish, and English.
Output: Updated ARB files, refactored `lib/pages/info_page.dart`, and comprehensive widget test suite in `test/pages/info_page_test.dart`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-practical-aac-guidance-info/03-CONTEXT.md
@.planning/phases/03-practical-aac-guidance-info/03-RESEARCH.md
@.planning/phases/03-practical-aac-guidance-info/03-VALIDATION.md
@lib/pages/info_page.dart
@lib/l10n/intl_fi.arb
@lib/l10n/intl_sv.arb
@lib/l10n/intl_en.arb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define localized ARB keys for Quick Start and Practical AAC Use Cases (D-04, GUIDE-01, GUIDE-02)</name>
  <files>lib/l10n/intl_fi.arb, lib/l10n/intl_sv.arb, lib/l10n/intl_en.arb</files>
  <read_first>lib/l10n/intl_fi.arb, lib/l10n/intl_sv.arb, lib/l10n/intl_en.arb</read_first>
  <action>
Add the following localization keys to `intl_fi.arb`, `intl_sv.arb`, and `intl_en.arb`:

1. Quick Start:
   - `infoQuickStartTitle`: FI "Pikaopas", SV "Snabbguide", EN "Quick Start"
   - `infoQuickStartSubtitle`: FI "Näin aloitat Kuvarin käytön arjen kommunikoinnissa:", SV "Så här kommer du igång med Kuvari i vardagskommunikationen:", EN "How to get started with Kuvari for visual communication:"
   - `infoQuickStartStep1Title`: FI "1. Etsi kuvia", SV "1. Sök bilder", EN "1. Search symbols"
   - `infoQuickStartStep1Desc`: FI "Kirjoita hakusana (esim. ruoka, pukeutuminen tai tunne) hakukenttään ja valitse sopivat kuvat.", SV "Skriv ett sökord (t.ex. mat, kläder eller känslor) i sökrutan och välj passande bilder.", EN "Type a keyword (e.g., food, clothing, or emotion) in the search field and select relevant symbols."
   - `infoQuickStartStep2Title`: FI "2. Kokoa jonoon", SV "2. Samla i sekvens", EN "2. Add to queue"
   - `infoQuickStartStep2Desc`: FI "Valitsemasi kuvat kertyvät etusivun jonomuotoiselle alustalle viestiksi tai päiväjärjestykseksi.", SV "Dina valda bilder samlas i kön på startsidan för att bilda ett meddelande eller ett schema.", EN "Selected symbols collect in the queue on the home screen to build a message or schedule."
   - `infoQuickStartStep3Title`: FI "3. Näytä tai kuuntele", SV "3. Visa eller lyssna", EN "3. View or listen"
   - `infoQuickStartStep3Desc`: FI "Avaa kuvajono katseluun painamalla \"Näytä kuvajono\" tai kuuntele sana painamalla kaiutinkuvaketta.", SV "Öppna sekvensen genom att trycka på \"Visa bildsekvens\" eller lyssna på ordet via högtalarikonen.", EN "Open the reel by tapping \"View story\" or listen to the spoken word by tapping the speaker icon."

2. Practical Use Cases (GUIDE-01):
   - `infoUseCasesTitle`: FI "Käytännön tilanteet", SV "Praktiska situationer", EN "Practical Situations"
   - `infoUseCasesSubtitle`: FI "Vinkkejä AAC-kommunikointiin arjen vuorovaikutuksessa:", SV "Tips för AKK-kommunikation i vardagen:", EN "AAC communication strategies for everyday interactions:"
   - `infoUseCaseChoiceTitle`: FI "Valinnan tekeminen", SV "Att göra val", EN "Making Choices"
   - `infoUseCaseChoiceDesc`: FI "Lisää jonoon 2–3 vaihtoehtoa (esim. mehu tai vesi, ulkoilu tai peli). Anna viestijälle rauhassa aikaa osoittaa haluamaansa kuvaa ja vahvista valinta ääneen.", SV "Lägg till 2–3 alternativ i sekvensen (t.ex. saft eller vatten, utelek eller spel). Ge kommunikatören gott om tid att peka och bekräfta valet verbalt.", EN "Add 2–3 symbols to the queue (e.g., juice or water, outdoor play or game). Give the communicator ample time to point or choose, then verbally confirm."
   - `infoUseCaseRoutineTitle`: FI "Päiväjärjestys ja toimintaketjut", SV "Dagsordning och rutiner", EN "Daily Routines & Sequences"
   - `infoUseCaseRoutineDesc`: FI "Järjestä peräkkäiset toiminnot aikajärjestykseen (esim. aamutoimet tai siirtymät). Ennakoitavuus vähentää epävarmuutta ja tukee arjen sujuvuutta.", SV "Ordna aktiviteter i kronologisk följd (t.ex. morgonrutin eller övergångar). Förutsägbarhet skapar trygghet och struktur i vardagen.", EN "Arrange consecutive activities in chronological order (e.g., morning routine, therapy transitions). Predictability provides structure and lowers anxiety."
   - `infoUseCaseSpeechTitle`: FI "Osoittaminen ja puhesynteesi", SV "Pekning och talsyntes", EN "Pointing & Speech Modeling"
   - `infoUseCaseSpeechDesc`: FI "Käytä puhepainiketta samalla kun osoitat kuvaa. Kuvan ja puhutun sanan yhdistäminen tukee puheen ymmärtämistä ja sanaston oppimista.", SV "Tryck på talsyntesknappen samtidigt som du pekar på bilden. Att kombinera bild och talat ord stödjer språkförståelse och inlärning.", EN "Use the speech button while pointing to the symbol. Pairing spoken words with visual symbols reinforces language modeling and comprehension."

3. About & Attributions:
   - `infoAboutTitle`: FI "Tietoa ja lähteet", SV "Information och källor", EN "About & Attributions"

Run `flutter gen-l10n` to regenerate Dart localization classes.
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test</automated>
    <fails_when>non-zero exit code or generation syntax errors</fails_when>
  </verify>
  <acceptance_criteria>
    - All 15 new localization keys exist with matching metadata in `intl_fi.arb`, `intl_sv.arb`, and `intl_en.arb`.
    - `AppLocalizations` generated successfully and accessible without compilation errors.
    - All existing tests continue to pass.
  </acceptance_criteria>
  <done>Localization keys are defined in all three languages and generated into AppLocalizations.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor InfoPage into a scrollable 3-Card layout with optional URL launcher injection (D-01, D-02, D-03, D-05, GUIDE-01, GUIDE-02)</name>
  <files>lib/pages/info_page.dart</files>
  <read_first>lib/pages/info_page.dart</read_first>
  <action>
Refactor `lib/pages/info_page.dart`:
1. Constructor:
   ```dart
   class InfoPage extends StatelessWidget {
     final Future<bool> Function(Uri url)? urlLauncher;

     const InfoPage({super.key, this.urlLauncher});
   ```
2. Internal helper:
   ```dart
   Future<void> _handleLaunchUrl(BuildContext context, String url) async {
     final Uri uri = Uri.parse(url);
     final launcher = urlLauncher ?? (u) => launchUrl(u, mode: LaunchMode.externalApplication);
     try {
       final success = await launcher(uri);
       if (!success && context.mounted) {
         ScaffoldMessenger.of(context).showSnackBar(
           SnackBar(content: Text('${AppLocalizations.of(context)!.searchError} $url')),
         );
       }
     } catch (e) {
       if (context.mounted) {
         ScaffoldMessenger.of(context).showSnackBar(
           SnackBar(content: Text('${AppLocalizations.of(context)!.searchError} $e')),
         );
       }
     }
   }
   ```
3. Layout Structure:
   - Wrap body in `SafeArea(child: ListView(padding: const EdgeInsets.all(16.0), children: [ ... ]))`.
   - **Card 1: Quick Start (Pikaopas)**:
     - Header row: teal circular container with `Icon(Icons.bolt_outlined, color: Colors.teal)`, `Text(l10n.infoQuickStartTitle, style: bold titleLarge)`.
     - Subtitle: `Text(l10n.infoQuickStartSubtitle)`.
     - 3 step rows with circular badge numbers (1, 2, 3 in teal background/circle) + step title and description.
   - **Card 2: Practical Use Cases (Käytännön tilanteet)**:
     - Header row: teal circular container with `Icon(Icons.forum_outlined, color: Colors.teal)`, `Text(l10n.infoUseCasesTitle, style: bold titleLarge)`.
     - Subtitle: `Text(l10n.infoUseCasesSubtitle)`.
     - Item 1: `Icons.check_circle_outline` + `l10n.infoUseCaseChoiceTitle` + `l10n.infoUseCaseChoiceDesc`.
     - Divider.
     - Item 2: `Icons.schedule_outlined` + `l10n.infoUseCaseRoutineTitle` + `l10n.infoUseCaseRoutineDesc`.
     - Divider.
     - Item 3: `Icons.record_voice_over_outlined` + `l10n.infoUseCaseSpeechTitle` + `l10n.infoUseCaseSpeechDesc`.
   - **Card 3: About & Attributions (Tietoa ja lähteet)**:
     - Header row: teal circular container with `Icon(Icons.info_outline, color: Colors.teal)`, `Text(l10n.infoAboutTitle, style: bold titleLarge)`.
     - Paragraph 1: `l10n.infoPageParagraph1`.
     - Paragraph 2: `l10n.infoPageParagraph2`.
     - Link items (interactive rows with `Icons.link`, underlined localized text, and trailing `Icons.open_in_new`):
       - Papunet kuvapankki (`l10n.papunetLinkLabel`) -> `https://papunet.net/kuvatyokalut/kuvapankki/`
       - OpenSymbols (`l10n.openSymbolsLinkLabel`) -> `https://www.opensymbols.org/`
       - Rinnekodit Oy (`l10n.rinnekoditLinkLabel`) -> `https://www.rinnekodit.fi/`
       - License (`l10n.licenseLinkLabel`) -> `https://creativecommons.org/licenses/by-nc-sa/4.0/` (localized deed suffix if applicable).
4. Styling details:
   - Cards styled with `RoundedRectangleBorder(borderRadius: BorderRadius.circular(12.0), side: BorderSide(color: Colors.teal.shade100))`.
   - Card padding `EdgeInsets.all(16.0)`.
   - Separation `SizedBox(height: 16.0)` between cards.
  </action>
  <verify>
    <automated>flutter analyze lib/pages/info_page.dart</automated>
    <fails_when>non-zero exit code or analyzer warnings</fails_when>
  </verify>
  <acceptance_criteria>
    - `InfoPage` renders inside a scrollable `ListView`.
    - 3 distinct Material 3 `Card` sections are displayed.
    - Card headers include teal icons (`bolt_outlined`, `forum_outlined`, `info_outline`).
    - Practical use cases for choice-making, daily routines, and speech modeling are included.
    - Attributions and links to Papunet, OpenSymbols, Rinnekodit, and Creative Commons are preserved and interactive.
    - Zero analyzer warnings.
  </acceptance_criteria>
  <done>InfoPage is refactored into a clean, scrollable 3-Card Material 3 layout with complete AAC practical guidance.</done>
</task>

<task type="auto">
  <name>Task 3: Implement comprehensive widget test suite for InfoPage (GUIDE-01, GUIDE-02, D-01..D-05)</name>
  <files>test/pages/info_page_test.dart</files>
  <read_first>test/pages/info_page_test.dart</read_first>
  <action>
Create `test/pages/info_page_test.dart`:
1. Widget Test 1: Layout and Card Structure:
   - Pumps `InfoPage` in `MaterialApp` with Finnish locale.
   - Verifies `ListView` is present.
   - Verifies exactly 3 `Card` widgets are present.
   - Verifies icons: `Icons.bolt_outlined`, `Icons.forum_outlined`, `Icons.info_outline`.
2. Widget Test 2: Quick Start Content:
   - Verifies presence of Quick Start title, and all three steps (1. Etsi kuvia, 2. Kokoa jonoon, 3. Näytä tai kuuntele).
3. Widget Test 3: Practical Use Cases Content (GUIDE-01):
   - Verifies presence of Practical Situations title and all three use case titles: "Valinnan tekeminen", "Päiväjärjestys ja toimintaketjut", "Osoittaminen ja puhesynteesi".
4. Widget Test 4: Attributions and Interactive URL Launching (D-05):
   - Injects custom `urlLauncher`:
     ```dart
     final launched = <String>[];
     await tester.pumpWidget(MaterialApp(
       localizationsDelegates: AppLocalizations.localizationsDelegates,
       supportedLocales: AppLocalizations.supportedLocales,
       locale: const Locale('fi'),
       home: InfoPage(urlLauncher: (uri) async {
         launched.add(uri.toString());
         return true;
       }),
     ));
     ```
   - Scrolls down and taps each link:
     - Papunet link -> verifies `launched.contains('https://papunet.net/kuvatyokalut/kuvapankki/')`
     - OpenSymbols link -> verifies `launched.contains('https://www.opensymbols.org/')`
     - Rinnekodit link -> verifies `launched.contains('https://www.rinnekodit.fi/')`
     - Creative Commons link -> verifies `launched.any((u) => u.contains('creativecommons.org'))`
5. Widget Test 5: Multi-Locale Rendering (D-04):
   - Pumps `InfoPage` under `Locale('sv')` -> verifies Swedish titles: "Snabbguide", "Praktiska situationer", "Information och källor".
   - Pumps `InfoPage` under `Locale('en')` -> verifies English titles: "Quick Start", "Practical Situations", "About & Attributions".
  </action>
  <verify>
    <automated>flutter test test/pages/info_page_test.dart</automated>
    <fails_when>non-zero exit code or failing tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `test/pages/info_page_test.dart` passes all tests cleanly.
    - Confirms scrollable layout, 3 cards, guidance content, all link dispatches, and multi-language support (fi, sv, en).
    - `flutter test` across entire project passes with zero failures.
  </acceptance_criteria>
  <done>Comprehensive widget test suite validates all requirements for Phase 3.</done>
</task>

</tasks>
